import Button from '@material-ui/core/button'

const button = () => {
    return (
        <div>
            <Button>
                submt
            
            </Button>
        </div>
    )
}

export default button
